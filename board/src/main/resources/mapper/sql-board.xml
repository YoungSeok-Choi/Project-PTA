<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 매퍼의 네임스페이스를 지정, '매퍼 인터페이스 경로 == 메서드 이름 == 쿼리의 이름', 공통적으로 사용되는 경로를 지정해서 네임스페이스와 쿼리 아이디가 합쳐 호출되게 한다. -->
<mapper namespace="board.board.mapper.BoardMapper"> 
	<!-- select 태그를 이용하여 select 쿼리임을 나타낸다. id는 해당 쿼리의 아이디를 지정. resultType은 BoardDto 형식으로 반환. 타입 별칭! -->
	<select id="selectBoardList" resultType="board.board.dto.BoardDto">
		<![CDATA[
			SELECT
				board_idx,
				title,
				hit_cnt,
				created_datetime
			FROM
				t_board
			WHERE
				deleted_yn = 'N'
			ORDER BY board_idx DESC
		]]>
	</select>
	
	<!-- '#변수'는 DTO의 get변수 메서드로, 해당하는 변수값을 가져온다. -->
	<insert id="insertBoard" parameterType="board.board.dto.BoardDto">
		<![CDATA[
			INSERT INTO t_board( title, contents, created_datetime, creator_id) VALUES (#{title}, #{contents}, NOW(), 'admin') 
		]]>
	</insert>
	
	<update id="updateHitCount" parameterType="int">
		<![CDATA[
			UPDATE 
				t_board
			SET
				hit_cnt = hit_cnt + 1
			WHERE
				board_idx = #{boardIdx}
		]]>
	</update>
	<select id="selectBoardDetail" parameterType="int" resultType="board.board.dto.BoardDto">
		<![CDATA[
			SELECT 
				board_idx, title, contents, hit_cnt,
				created_datetime,
				creator_id
			FROM
				t_board
			WHERE
				board_idx = ${boardIdx} AND deleted_yn = 'N'
		]]>
	</select>
	
	<!-- updater가 아니라 updator 오타 -->
	<update id="updateBoard" parameterType="board.board.dto.BoardDto">
		<![CDATA[
			UPDATE t_board SET
			title=#{title}, contents=#{contents}, updated_datetime=NOW(), updator_id='admin'
			WHERE board_idx = #{boardIdx}
		]]>
	</update>
	
	<update id="deleteBoard" parameterType="int">
		<![CDATA[
			UPDATE t_board SET
			deleted_yn = 'Y', updated_datetime = NOW(), updator_id = 'admin'
			WHERE board_idx = #{boardIdx}
		]]>
	</update>
</mapper>